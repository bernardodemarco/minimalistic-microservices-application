generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Users {
  cpf          String @id
  name         String
  email        String @unique
  phone_number String @unique
}
